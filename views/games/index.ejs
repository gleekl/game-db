<%- include ('../partials/head.ejs') %>

<ul class="games-list">
    <% for (let game of games) { %>
        <div class="card" style="width: 18rem">
            <li class="card game">
                <a class="game-desc" href="<%= baseUrl %>/<%= game.id %>">
                    <figure><img class="card-img-top embed-responsive-item" src="<%= game.img %>"/></figure>
                    <div class="card-body">
                      <h4 class="card-title"><%= game.name %></h4>
                      <p class="card-text"><%= game.type %></p>
                    </div>
                  </a>
    
                <div class="action-bar">
                    <a class="btn btn-primary edit" href="<%= baseUrl %>/<%= game.id %>/edit">Edit</a>
                    <form action="<%= baseUrl %>/<%= game.id %>?_method=DELETE" method="post">
                    <input class="btn btn-danger" type="submit" value="Delete" />
                    </form>
                </div>
            </li>
        </div>
    <% } %>
</ul>

<%- include ('../partials/footer.ejs') %>